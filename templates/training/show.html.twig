{% extends 'base.html.twig' %}

{% block title %}Training{% endblock %}

{% block body %}
    <h1>Training</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Code</th>
                <td>{{ training.code }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ training.title }}</td>
            </tr>
            <tr>
                <th>Duration</th>
                <td>{{ training.duration }}</td>
            </tr>
            <tr>
                <th>Places</th>
                <td>{{ training.places }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_training_index') }}">back to list</a>

    <a href="{{ path('app_training_edit', {'code': training.code}) }}">edit</a>

    {{ include('training/_delete_form.html.twig') }}
{% endblock %}
