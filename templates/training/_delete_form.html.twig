<form method="post" action="{{ path('app_training_delete', {'code': training.code}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ training.code) }}">
    <button class="btn">Delete</button>
</form>
